# Launch checklist

## Before posting

- [ ] `pip install openeval` works in a clean venv
- [ ] `openeval --help` works
- [ ] `openeval run --suite factuality --model ollama:llama3.2` works (local)
- [ ] `openeval run --suite hallucination --model openai:gpt-4o-mini --async --concurrency 20` works (API)
- [ ] Demo GIF exists at `openeval/assets/demo.gif`
- [ ] Docs site is live (GitHub Pages) and linked in repo settings
- [ ] `openeval-action/README.md` is clear and action is tagged (e.g. `v1`)
- [ ] Benchmarks are committed (`openeval/benchmarks/results.json`) and README table matches
- [ ] Repo has a good description + topics (`llm`, `evaluation`, `testing`, `pytest`, `rag`, `ollama`)

## Suggested schedule (US/Eastern)

- Tue/Wed 8–10am: HN “Show HN”
- Tue 10–11am: LinkedIn (post GIF / screenshot)
- Tue noon: r/MachineLearning
- Tue afternoon: r/LocalLLaMA

